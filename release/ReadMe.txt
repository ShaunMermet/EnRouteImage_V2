//////////////////////
////			 /////
////   SETUP     /////
////			 /////
//////////////////////

1 - The .sql file attached contains the sql requests to setup the database. Run them to create the database
2 - Edit the .htaccess.exemple file and change variables to fit environment

	DB_HOST
	DB_NAME
	DB_USER
	DB_PASSWORD
	SMTP_HOST
	SMTP_USER
	SMTP_PASSWORD

	Save the file as .htaccess (i.e. change name)

3 - Deploy files on the server keeping the same structure

The entry point is the index.php file.